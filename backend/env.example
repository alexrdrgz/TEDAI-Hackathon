# Gemini API Configuration
GEMINI_API_KEY=your_gemini_api_key_here

# Google Cloud Text-to-Speech API credentials
# Create a service account at https://console.cloud.google.com/
# Enable Text-to-Speech API and download the JSON key file
GOOGLE_APPLICATION_CREDENTIALS=/path/to/your/service-account-key.json

# Local Voice Service Configuration (Optional)
# Faster Whisper model size: tiny, base, small, medium, large-v2
WHISPER_MODEL=base
# Device: cpu or cuda (if you have GPU)
WHISPER_DEVICE=cpu

# Server Configuration
# Using port 3001 to avoid conflicts with common development servers
# Common ports to avoid: 3000 (React), 5173 (Vite), 8080 (many services), 5000 (Flask)
PORT=3030
NODE_ENV=development

# CORS Configuration
# Comma-separated list of allowed origins for CORS
# Backend runs on 3001, frontend typically on 5173 (Vite) or 3000 (CRA)
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000,http://localhost:3001

# Production example:
# ALLOWED_ORIGINS=https://your-frontend.com,https://app.your-domain.com

# Database Configuration
# SQLite database will be created automatically in backend/data/database.db

